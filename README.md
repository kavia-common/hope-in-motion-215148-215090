# hope-in-motion-215148-215090

This workspace includes the Evobike short film React frontend.

Important:
- Place the video file at `evobike_shortfilm_frontend/public/assets/evobike_placeholder.mp4` (placeholder name expected by the app).
- If using a different host, set `REACT_APP_FRONTEND_URL` so the app resolves `${REACT_APP_FRONTEND_URL}/assets/evobike_placeholder.mp4`.
- Update scene timestamps in `src/App.js` if the final edit differs.